using System;
// Creativity = I made the eternal goal a daily check off goal and will show if it's ccompleted based off if it was recorded the same day that it's being assessed for completed. Added a "help" option for the menu options
class Program
{
    static void Main(string[] args)
    {
        GoalManager start = new GoalManager();
        start.Start();
    }
}